[project]
name = "helio"
version = "0.1.0"
description = "Detector de sonolência e distrações para automóveis."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "opencv-python",
    "mediapipe",
    "scipy",
    "numpy",
]
